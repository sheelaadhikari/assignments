#include <stdio.h>
void main()
{
    int n, i;
    printf("enter a number: ");
    scanf("%d", &n);
    for (i = 2; i = n / 2; i++)

    {
        if (n % 2 == 0)
            
        {

            printf("the number is not prime");
        }

        else
        {
            printf("the number is prime");
        }
    }
}